mutation updateNotificationsLastSent(
  $ids: [Int!]
  $lastSent: timestamptz
  ) {
  update_notification(
    where: { id: {_in: $ids} }
    _set: {
    last_sent: $lastSent
  }) {
    affected_rows
  }
}
