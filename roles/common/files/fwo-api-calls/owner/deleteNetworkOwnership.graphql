mutation deleteNetworkOwnership(
  $ownerId: Int!
  $id: bigint!
  ) {
  update_owner_network(where: {owner_id: {_eq: $ownerId}, id: {_eq: $id}}
    _set: {is_deleted: true}) {
    affected_rows
  }
}
