mutation setNetworkOwnershipDeleted(
  $ownerId: Int!
  $id: bigint!
  $deleted: Boolean!
) {
  update_owner_network(
    where: { owner_id: { _eq: $ownerId }, id: { _eq: $id } }
    _set: { is_deleted: $deleted }
  ) {
    affected_rows
  }
}