mutation setAppServerRemovedState(
  $id: bigint!
  $removed: Boolean!
  ) {
  update_owner_network_by_pk(
    pk_columns: { id: $id }
    _set: {
      removed: $removed
  }) {
    UpdatedIdLong: id
  }
}
